#pragma once

#ifndef _JNI_COCOS_BITMAP_H
#define _JNI_COCOS_BITMAP_H

#include "../environment.h"

void jni_create_text_bitmap(Environment& env, std::uint32_t java_env, std::uint32_t local_ref, std::uint32_t method_id) {
	// i don't think this method is actually used for anything in gd
}

#endif